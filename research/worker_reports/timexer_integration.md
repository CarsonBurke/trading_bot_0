# TimeXer integration: ReturnSpace main tree + timexer-exog branch (b8ce968e)

`git apply --reject` of `git diff bfd3404c..b8ce968e` landed features.rs, mod.rs, dataset.rs (`et_offset_secs` pub(crate)), campaign script and ~half the hunks; the rest were ported by hand:
- corpus.rs: `use` block (+`bar_file_path`, `features::*`, `file_sha256`), `RESOLUTION_MS` pub(super), SCHEMA gets `;exogenous-variates-on-shared-utc-grid` appended to main's v2 string, `host_batch` width (`context * features.channels()`), `fill_row` (aux `AuxiliaryCursor` write inside main's second loop under `position < context`; σ/c_last pass untouched), tests `pooled_rows_preserve_ticker_values_and_mask_partial_targets` (FeatureSet volume-only + `Exogenous::default()` args) and `pooled_epoch_...` (branch's 10-channel aux assertions added after main's σ-scaled input assertions).
- data.rs: `DataContract` loses `volume_features`/`auxiliary_schema`, `auxiliary_schema()` fn deleted, all `filtered_contract` test calls drop the bool (main had already dropped the parameter).
- model.rs: `use super::features::FeatureSet`, `ModelConfig.features` (+Default `ALL`), `normalize_auxiliary` → `normalize_innovation` rename (branch's group-wise `normalize_auxiliary(aux,&FeatureSet)` and variate projection had applied cleanly), `forward_with_aux` asserts `[batch, seq_len, features.channels()]`. Decision: deleted the aux-less `SegmentModel::forward` wrapper (only caller was the CUDA test; with default `features=all` it would trip the config-mismatch assert) and the CUDA test now feeds a random `[2, seq_len, 12]` auxiliary tensor via `forward_with_aux`, so the variate branch is exercised.
- runner.rs: manifest test `DataContract` literal and `FeatureSet` import (Manifest::read `model.features == data.features` check and both `Corpus::load` call sites applied cleanly). benchmark.rs: `aux_channels = args.model.features.channels()`.
- docs/timexer_segment.md: exogenous section inserted between encoder and a new "Decoder and objective" heading; intro/manifest/campaign sentences merged; no legacy/--volume-features/--project-candles/scaler-era text left (the ReturnSpace sentence stating there is *no* per-ticker scaler / window normalization was kept as design rationale).
- Row layout (`fill_row`, width = `(context+pred_len)*4 + context*n_aux + pred_len + 3`): inputs `[context,4]` then targets `[pred_len,4]` as `ln(p/c_last)/σ`; aux `[context, n_aux]` with `n_aux = 2 × enabled features` in fixed order time-of-day(sin,cos), day-of-week(sin,cos), session-gap(flag, ln(Δmin/5)≤8), volume(log-innovation, valid), market(mean 5-min log return, valid), spy(log return, valid) → 12 for `all`; target mask `pred_len` floats (first `targets` = 1); geometry_context `[c_last, σ, mean_rel_range]`. `Batch::from_packed(packed, context, pred_len, aux_channels, valid_target_bars)`.
- Manifest: FORMAT `timexer-ohlc-universe-v4`, OBJECTIVE `logreturn_sigma_mse_v1`; `CorpusContract{features, auxiliary_schema, spy_fingerprint}` and `ModelConfig.features` have no serde defaults (`deny_unknown_fields`), so pre-feature or other-objective manifests are rejected.
- Verification: `./torch-env.sh cargo check --tests` → trading_bot_0 0 errors (0 warnings in timexer_segment), `--manifest-path tui/Cargo.toml` 0, `-p shared` 0; `./torch-env.sh cargo test -p trading_bot_0 --lib timexer_segment` → 26 passed, 0 failed (CUDA test gated behind `TIMEXER_SEGMENT_GPU_TEST=1`, not run: no GPU work). Straggler grep (`legacy|project_candles|volume_features|DecoderKind|geometry::|close_mean|global_mean|price_scaling|Dataset`) over timexer_segment, main.rs, dataset.rs, campaign script, docs → zero hits (main.rs `..._legacy_commands` test names old CLI subcommands, unrelated). Worktree/branch untouched, nothing committed.
- Train CLI: `mlq submit --name timexer-universe --max-parallel-runs 1 --time-limit 12h --cwd "$PWD" -- ./trading_bots/run-release-cuda.sh train-timexer-segment --run timexer-universe --features all` (`--features` is a comma list of `time-of-day,day-of-week,session-gap,volume,market,spy`, or `all`/`none`; `spy` requires `long_data/bars/SPY.300.bars`).
